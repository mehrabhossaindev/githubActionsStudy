name: Dependency Between Jobs

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build your project
        run: echodd "Build your project"

      - name: Checkout Project
        run: echo "Checkout this project"

  deploy:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - name: Deploy your Project
        run: echo "Project Deploy"
        
